---
date: 2026-04-05-1200
rdd: RDD-2026-04-05-1200.md
---

# システム設計書 - Search/Playlist Track Separation
**RDD:** `RDD-2026-04-05-1200.md`
**作成日:** 2026-04-05

## 1. 全体方針

- `search_tracks` を `RenderContext` に追加し、SearchScreen が `g_tracks` を使わないようにする。`g_tracks` は PlaylistDetailScreen 専用として残す（リネームしない）。
- SearchScreen の `draw_bottom` / `update()` を PlayingScreen 構造に揃えるため、PlayingScreen のリスト描画ループを `draw_playing_style_list()` に抽出し、両画面から呼ぶ。
- POPUP_PLAYLIST_ADD でのトラック特定を `selected_track_id` による検索に変更（複数リスト対応）。

---

## 2. 新規関数（既存ファイルへの追加）

### `track_list_helpers.h` — 宣言追加

```cpp
// Playing-style list: used by PlayingScreen and SearchScreen
// Draws tracks with PlayingScreen item style (30px/item, y=8..200)
void draw_playing_style_list(
    const std::vector<Track>& tracks,
    const RenderContext& ctx,
    UIManager& ui_mgr
);
```

### `track_list_helpers.cpp` — 実装追加

PlayingScreen の `draw_bottom` からリスト描画ループを切り出す。
使用定数: `BTM_LIST_START_Y=8.0f`、`BTM_ITEM_HEIGHT_2ROW=30.0f`、`PlayBar::BAR_Y=200.0f`

```cpp
void draw_playing_style_list(
    const std::vector<Track>& tracks,
    const RenderContext& ctx,
    UIManager& ui_mgr)
{
    if (tracks.empty()) return;
    C2D_Text text;
    C2D_TextBuf buf = ui_mgr.get_text_buf();

    constexpr float START_Y = BTM_LIST_START_Y;      // 8.0f
    constexpr float END_Y   = PlayBar::BAR_Y;         // 200.0f
    constexpr float H       = BTM_ITEM_HEIGHT_2ROW;   // 30.0f

    int scroll_items; float y_shift;
    calc_scroll(ctx.scroll_offset_y, H, scroll_items, y_shift);
    float y_base = START_Y - y_shift;
    int first    = scroll_items;
    int visible  = (int)((END_Y - START_Y) / H) + 1;

    for (int i = first; i < first + visible; ++i) {
        if (i < 0 || i >= (int)tracks.size()) continue;
        float y_item = y_base + (i - first) * H;
        if (y_item + H < START_Y || y_item >= END_Y) continue;

        const Track& tr  = tracks[i];
        bool is_playing  = (tr.id == ctx.playing_id);
        bool is_selected = (i == ctx.selected_index);

        if (is_selected) {
            C2D_DrawRectSolid(0, y_item, 0, 320, H, ctx.theme->accent);
            draw_selection_left_bar(y_item, H, ctx.theme->accent);
        } else if (is_playing) {
            C2D_DrawRectSolid(0, y_item, 0, 320, H, ctx.theme->playing_bg);
        } else {
            draw_item_bg(0, y_item, 320, H, ctx.theme->bg_bottom);
        }

        u32 title_color = (is_selected || is_playing)
                          ? ctx.theme->accent_text : ctx.theme->text_body;

        std::string display_title = is_playing ? (">> " + tr.title) : tr.title;
        C2D_TextParse(&text, buf, display_title.c_str());
        {
            float draw_x = BTM_MARGIN_X;
            if (is_selected) {
                float text_w    = text.width * FONT_SM;
                float display_w = 320.0f - BTM_MARGIN_X * 2;
                float eff_scroll = (text_w > display_w)
                    ? std::min((float)ctx.scroll_x, text_w - display_w) : 0.0f;
                draw_x = BTM_MARGIN_X - eff_scroll;
            }
            C2D_DrawText(&text, C2D_WithColor, draw_x, y_item, 0,
                         FONT_SM, FONT_SM, title_color);
        }
        if (!tr.duration.empty()) {
            C2D_TextParse(&text, buf, tr.duration.c_str());
            C2D_DrawText(&text, C2D_WithColor,
                         BTM_MARGIN_X + 8, y_item + BTM_META_OFFSET, 0,
                         FONT_XS, FONT_XS, title_color);
        }
        if (!is_selected) {
            u32 sep = ctx.theme->text_dim & 0x14FFFFFF;
            draw_dashed_line_h(0, y_item + H - 1, 320, sep);
        }
    }
}
```

---

## 3. 既存ファイルの変更

### `source/ui/ui_renderer.h`

`RenderContext` に1フィールド追加（`g_tracks` の直後）:

```cpp
// Before:
std::vector<Track> g_tracks;
std::vector<Track> playing_tracks;

// After:
std::vector<Track> g_tracks;
std::vector<Track> search_tracks;   // ← 追加（SearchScreen 専用）
std::vector<Track> playing_tracks;
```

> **`make clean` 必須**（RenderContext の ABI が変わるため。coding-patterns #14）

---

### `source/main.cpp`

**① 検索完了（~line 240）**
```cpp
// Before:
ctx.g_tracks = res;
// After:
ctx.search_tracks = res;
```

**② 検索開始・キーボードクローズ（~line 1753）**
```cpp
// Before:
ctx.g_tracks.clear();
// After:
ctx.search_tracks.clear();
```

**③ STATE_SEARCH — KEY_X ポップアップ（~lines 845-851）**
```cpp
// Before:
if (!ctx.g_tracks.empty() &&
    ctx.g_tracks[ctx.selected_index].id != "SEARCH_BTN") {
    ctx.selected_track_id = ctx.g_tracks[ctx.selected_index].id;
// After:
if (!ctx.search_tracks.empty()) {
    ctx.selected_track_id = ctx.search_tracks[ctx.selected_index].id;
```

**④ `start_playback` アクションハンドラ（~line 993）**
```cpp
// Before:
start_playback(ctx.g_tracks[ctx.selected_index]);
// After:
start_playback(ctx.search_tracks[ctx.selected_index]);
```

**⑤ シークフォールバック（~lines 1082-1086）**
```cpp
// Before:
if (!seek_valid) {
    for (const auto& t : ctx.g_tracks) {
        if (t.id == ctx.playing_id) { seek_track = t; seek_valid = true; break; }
    }
}
// After:
if (!seek_valid) {
    for (const auto& t : ctx.search_tracks) {
        if (t.id == ctx.playing_id) { seek_track = t; seek_valid = true; break; }
    }
}
```

**⑥ POPUP_PLAYLIST_ADD — トラック特定ロジック（~lines 1362-1372）**

`ctx.g_tracks[ctx.selected_index]` をハードコードするのではなく、`selected_track_id` で全リストを検索する形に変更。PlayingScreen 経由でのポップアップも正しく動作するようになる副次効果あり。

```cpp
// Before:
playlist_manager.add_track(pl_id, ctx.g_tracks[ctx.selected_index]);
// (same pattern for "new playlist" branch)

// After: (同一ロジックを両ブランチに適用)
Track track_to_add;
{
    auto find = [&](const std::vector<Track>& v) -> bool {
        for (auto& t : v)
            if (t.id == ctx.selected_track_id) { track_to_add = t; return true; }
        return false;
    };
    find(ctx.playing_tracks) || find(ctx.search_tracks) || find(ctx.g_tracks);
}
playlist_manager.add_track(pl_id, track_to_add);
```

---

### `source/ui/ui_renderer.cpp`

TRACK_DETAILS フォールバック検索に `search_tracks` を追加（~lines 307-310）:

```cpp
// Before:
if (!found_track) {
    for (const auto& t : ctx.g_tracks) {
        if (t.id == ctx.selected_track_id) { found_track = &t; break; }
    }
}

// After:
if (!found_track) {
    for (const auto& t : ctx.search_tracks) {
        if (t.id == ctx.selected_track_id) { found_track = &t; break; }
    }
}
if (!found_track) {
    for (const auto& t : ctx.g_tracks) {
        if (t.id == ctx.selected_track_id) { found_track = &t; break; }
    }
}
```

---

### `source/ui/screens/playing_screen.cpp`

`draw_bottom` のリストループを `draw_playing_style_list` 呼び出しに置き換え:

```cpp
void PlayingScreen::draw_bottom(const RenderContext& ctx, UIManager& ui_mgr) {
    // --- Track list ---
    draw_playing_style_list(ctx.playing_tracks, ctx, ui_mgr);   // ← 置き換え

    // --- Play bar (unchanged) ---
    PlayBar::draw(ctx, ui_mgr);

    // --- HOME button (unchanged, PlayingScreen-specific) ---
    // ... existing HOME button drawing code (no change)
}
```

---

### `source/ui/screens/search_screen.cpp`

全面書き換え。以下に新しい全文を示す。

**`on_enter`**: `search_tracks` 先頭に selected_index を合わせる（PlayingScreen と同じパターン）。

```cpp
void SearchScreen::on_enter(AppContext& ctx) {
    ctx.current_state = STATE_SEARCH;
    ctx.scroll_offset_y = 0.0f;
    ctx.selected_index = 0;
    ctx.touch_state.reset();
}
```

**`draw_bottom`**:
```cpp
void SearchScreen::draw_bottom(const RenderContext& ctx, UIManager& ui_mgr) {
    draw_playing_style_list(ctx.search_tracks, ctx, ui_mgr);
    PlayBar::draw(ctx, ui_mgr);
    draw_menu_button(ctx, ui_mgr);
}
```

**`update`**: PlayingScreen の update() をベースに `playing_tracks` → `search_tracks`、`play_selected_track` → `start_playback` に変更。

```cpp
std::string SearchScreen::update(AppContext& ctx, u32 kDown, u32 kHeld, u32 kRepeat) {
    touchPosition touch;
    hidTouchRead(&touch);

    if (kDown & KEY_TOUCH)
        ctx.touch_state.begin(touch.px, touch.py);

    if ((kHeld & KEY_TOUCH) && ctx.touch_state.is_touching) {
        int delta = ctx.touch_state.update(touch.px, touch.py);
        if (ctx.touch_state.is_dragging &&
            ctx.touch_state.start_y >= (int)BTM_LIST_START_Y &&
            ctx.touch_state.start_y < (int)PlayBar::BAR_Y &&
            !ctx.search_tracks.empty()) {
            ctx.scroll_offset_y += (float)delta;
            int visible = (int)((PlayBar::BAR_Y - BTM_LIST_START_Y) / BTM_ITEM_HEIGHT_2ROW);
            float max_scroll = ((int)ctx.search_tracks.size() - visible) * BTM_ITEM_HEIGHT_2ROW;
            if (ctx.scroll_offset_y < 0.0f) ctx.scroll_offset_y = 0.0f;
            if (max_scroll <= 0.0f) ctx.scroll_offset_y = 0.0f;
            else if (ctx.scroll_offset_y > max_scroll) ctx.scroll_offset_y = max_scroll;
        }
    }

    if (!(kHeld & KEY_TOUCH) && ctx.touch_state.is_touching) {
        int tx = ctx.touch_state.current_x;
        int ty = ctx.touch_state.current_y;
        bool from_seekbar = ctx.touch_state.start_y >= (int)PlayBar::BAR_Y
                         && ctx.touch_state.start_y < (int)(PlayBar::BAR_Y + PlayBar::SEEK_H);
        bool was_tap = ctx.touch_state.end();

        if (from_seekbar) {
            int total_secs = PlayBar::parse_duration_secs(ctx.playing_duration);
            if (total_secs > 0 && !ctx.playing_id.empty()) {
                float ratio = (float)tx / 320.0f;
                if (ratio < 0.03f) ratio = 0.0f;
                if (ratio > 0.97f) ratio = 1.0f;
                ctx.seek_target_seconds = (ratio >= 1.0f) ? total_secs - 1 : (int)(ratio * total_secs);
                return "seek";
            }
        } else if (was_tap) {
            // Menu button
            MenuBtnRect btn = get_menu_btn_rect(ctx.config);
            if (tx >= btn.x && tx <= btn.x + btn.w &&
                ty >= btn.y && ty <= btn.y + btn.h)
                return "trigger_nav_menu";

            // PlayBar buttons
            if (ty >= (int)PlayBar::BAR_Y) {
                std::string bar_action = PlayBar::handle_touch(tx, ty, ctx);
                if (!bar_action.empty()) return bar_action;
            }

            // List item tap
            if (ty >= (int)BTM_LIST_START_Y && ty < (int)PlayBar::BAR_Y &&
                !ctx.search_tracks.empty()) {
                int tapped = (int)((ctx.scroll_offset_y + ty - BTM_LIST_START_Y)
                                   / BTM_ITEM_HEIGHT_2ROW);
                if (tapped >= 0 && tapped < (int)ctx.search_tracks.size()) {
                    ctx.selected_index = tapped;
                    ctx.scroll_x = 0;
                    if (ctx.playing_id == ctx.search_tracks[tapped].id)
                        return "toggle_pause";
                    return "start_playback";
                }
            }
        }
    }

    // D-pad navigation (mirrors PlayingScreen)
    LightLock_Lock(&ctx.lock);
    if (!ctx.search_tracks.empty()) {
        bool moved = false;
        if (kRepeat & KEY_DDOWN) {
            ctx.selected_index++;
            if (ctx.selected_index >= (int)ctx.search_tracks.size())
                ctx.selected_index = 0;
            ctx.scroll_x = 0;
            moved = true;
        }
        if (kRepeat & KEY_DUP) {
            ctx.selected_index--;
            if (ctx.selected_index < 0)
                ctx.selected_index = (int)ctx.search_tracks.size() - 1;
            ctx.scroll_x = 0;
            moved = true;
        }
        if (moved) {
            int visible = (int)((PlayBar::BAR_Y - BTM_LIST_START_Y) / BTM_ITEM_HEIGHT_2ROW);
            int scroll_items = (int)(ctx.scroll_offset_y / BTM_ITEM_HEIGHT_2ROW);
            if (ctx.selected_index < scroll_items)
                ctx.scroll_offset_y = ctx.selected_index * BTM_ITEM_HEIGHT_2ROW;
            else if (ctx.selected_index >= scroll_items + visible)
                ctx.scroll_offset_y = (ctx.selected_index - visible + 1) * BTM_ITEM_HEIGHT_2ROW;
            float max_scroll = ((int)ctx.search_tracks.size() - visible) * BTM_ITEM_HEIGHT_2ROW;
            if (ctx.scroll_offset_y < 0.0f) ctx.scroll_offset_y = 0.0f;
            if (max_scroll <= 0.0f) ctx.scroll_offset_y = 0.0f;
            else if (ctx.scroll_offset_y > max_scroll) ctx.scroll_offset_y = max_scroll;
        }
        if (kRepeat & KEY_DRIGHT) ctx.scroll_x += 50;
        if (kRepeat & KEY_DLEFT) {
            ctx.scroll_x -= 50;
            if (ctx.scroll_x < 0) ctx.scroll_x = 0;
        }
    }
    LightLock_Unlock(&ctx.lock);

    // KEY_A: play / toggle pause
    if (kDown & KEY_A) {
        LightLock_Lock(&ctx.lock);
        bool has = !ctx.search_tracks.empty();
        std::string sel_id = has ? ctx.search_tracks[ctx.selected_index].id : "";
        LightLock_Unlock(&ctx.lock);
        if (has) {
            if (ctx.playing_id == sel_id) return "toggle_pause";
            return "start_playback";
        }
    }

    return "";
}
```

---

## 4. 各機能の詳細設計

### FR-1/FR-2 フロー
```
[検索キーボード入力]
    ↓ main.cpp: ctx.search_tracks.clear()
    ↓ 検索スレッド起動
    ↓ 完了: ctx.search_tracks = res
SearchScreen::update() → ctx.search_tracks 参照
```

### FR-3 POPUP_PLAYLIST_ADD のトラック特定
```
KEY_X 押下（STATE_SEARCH）
    → ctx.selected_track_id = ctx.search_tracks[selected_index].id
    → STATE_POPUP_TRACK_OPTIONS

"追加" 選択
    → STATE_POPUP_PLAYLIST_ADD

プレイリスト確定
    → find(playing_tracks) || find(search_tracks) || find(g_tracks)
    → playlist_manager.add_track(pl_id, track_to_add)
```

### FR-4 SearchScreen 下画面レイアウト
```
y=  0: (上画面制御はUIRenderer)
y=  8: ─── リスト開始 (draw_playing_style_list) ───
         30px × 最大6アイテム表示
y=188: ─── 最終アイテム末尾 ───
y=200: ─── PlayBar 開始 ───
         seek bar (14px) + コントロール (26px)
y=206: ─── ハンバーガーメニュー (PlayBar上に重畳) ───
y=240: ─── 画面下端 ───
```

### FR-5 共有ヘルパー
- `draw_playing_style_list()` が PlayingScreen / SearchScreen 両方から呼ばれる
- `playing_screen.cpp` の static constexpr (LIST_START_Y 等) はそのまま残す（内部で未使用になる分はコンパイラが最適化）

---

## 5. 実装順序

| ステップ | 内容 | ビルド確認 |
|----------|------|------------|
| 1 | `ui_renderer.h` に `search_tracks` 追加 | `make clean && make` |
| 2 | `main.cpp` ①②（検索完了・開始の切り替え） | `make` |
| 3 | `main.cpp` ③④⑤⑥（popup/playback/seek） | `make` |
| 4 | `track_list_helpers` に `draw_playing_style_list` 追加 | `make` |
| 5 | `playing_screen.cpp` をヘルパー呼び出しに変更 | `make`・再生画面目視確認 |
| 6 | `search_screen.cpp` 全面書き換え | `make`・検索画面目視確認 |
| 7 | `ui_renderer.cpp` フォールバック追加 | `make` |
| 8 | 総合確認（検索→プレイリスト閲覧→検索に戻る） | 実機またはエミュレータ |

---

## 6. 影響ファイル一覧

| ファイル | 変更種別 | 内容 |
|----------|----------|------|
| `source/ui/ui_renderer.h` | 変更 | `search_tracks` フィールド追加 |
| `source/main.cpp` | 変更 | ①②③④⑤⑥ の箇所 |
| `source/ui/ui_renderer.cpp` | 変更 | TRACK_DETAILS フォールバックに `search_tracks` 追加 |
| `source/ui/track_list_helpers.h` | 変更 | `draw_playing_style_list` 宣言追加 |
| `source/ui/track_list_helpers.cpp` | 変更 | `draw_playing_style_list` 実装追加 |
| `source/ui/screens/playing_screen.cpp` | 変更 | リストループ → ヘルパー呼び出しに変更 |
| `source/ui/screens/search_screen.cpp` | 変更 | 全面書き換え（`search_tracks` 使用、PlayingScreen 構成） |
